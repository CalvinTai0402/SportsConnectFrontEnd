import React from 'react';

const SliderWrapper = ({ children }) => (
  <div className="py-6 px-0 overflow-hidden relative w-[97%] xl:w-full mx-auto">
    {children}
  </div>
);

export default SliderWrapper;
